#ifndef SABLE_NORMALIZE_H
#define SABLE_NORMALIZE_H

#include <string>
#include <locale>

namespace sable {

std::string normalize(const std::string& in);

} // namespace sable

#endif // SABLE_NORMALIZE_H
